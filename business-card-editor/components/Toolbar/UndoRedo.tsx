import React from 'react';

const UndoRedo: React.FC = () => {
  return (
    <div style={{marginTop: 12}}>
      <button>Undo</button>
      <button>Redo</button>
    </div>
  );
};

export default UndoRedo;
